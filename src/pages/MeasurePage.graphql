query measurementStats {
  measurementStats {
    totalMeasurements
    totalWebsiteHosts
    totalAccessibilityViolations
  }
}

query measurementStatus($id: String!) {
  measurement(id: $id) {
    id
    status
    url
    host {
      host
    }
  }
}
