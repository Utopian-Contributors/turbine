query websites($query: String) {
  websites(query: $query) {
    id
    host
    ratings {
      overallScore
      createdAt
    }
    latestMeasurement {
      icon
      thumbnail
      description
      title
      url
      redirect
    }
  }
}
