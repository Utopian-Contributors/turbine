query websites($query: String) {
  websites(query: $query) {
    id
    host
    rating {
      overallScore
      createdAt
    }
    latestMeasurement {
      icon
      thumbnail
      description
      title
      url
      redirect
    }
  }
}
