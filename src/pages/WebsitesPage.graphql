query websites($query: String, $pagination: PaginationInput) {
  websites(query: $query, pagination: $pagination) {
    id
    host
    ratings {
      overallScore
      createdAt
    }
    icon
    thumbnail
    title
    description
    rootMeasurement {
      id
      url
      redirect
    }
  }
}
