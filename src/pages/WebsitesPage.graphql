query websites($query: String) {
  websites(query: $query) {
    id
    host
    ratings {
      overallScore
      createdAt
    }
    rootMeasurement {
      id
      icon
      thumbnail
      description
      title
      url
      redirect
    }
  }
}
