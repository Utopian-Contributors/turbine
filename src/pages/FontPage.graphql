query font($name: String!) {
  font(name: $name) {
    id
    name
    category
    tags
    menu
    integrated
    variants
    files {
      variant
      url
    }
  }
}

mutation toggleFontIntegration($id: String!) {
  toggleFontIntegration(id: $id) {
    id
    integrated
  }
}
